# Copy this file to .env.prod on the ECS host.
# Do not commit the real .env.prod file.

AI_BIOWORKFLOW_RUN_BACKEND=disabled
WDL_VALIDATOR=miniwdl

# Required only when natural-language planning is enabled.
# DEEPSEEK_API_KEY=sk-your-real-key

# Optional Cromwell settings for a future execution-enabled deployment.
# AI_BIOWORKFLOW_RUN_BACKEND=cromwell
# CROMWELL_URL=http://cromwell:8000
